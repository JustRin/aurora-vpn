<?xml version="1.0" encoding="utf-8"?>
<!-- 2×1 by default: button, state and speed. Resizable in both directions;
     the renderer picks a richer layout as the widget grows. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_compact_desc"
    android:initialLayout="@layout/widget_compact"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:previewImage="@drawable/widget_preview_compact"
    android:previewLayout="@layout/widget_compact"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
